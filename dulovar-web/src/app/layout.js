import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Dulovar | Interpol Alert Simulation with Microservices & Peer-to-Peer Architecture",
  description: "Explore Dulovar, an academic project simulating Interpol alert systems using React frontends, Go microservices with gRPC, and Rust-based peer-to-peer architecture.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
